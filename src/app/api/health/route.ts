import {NextResponse} from "next/server";import {db} from "@/lib/db";import {getQueue} from "@/lib/queue";export const dynamic="force-dynamic";export async function GET(){try{await db.query("SELECT 1");const q=await getQueue();await q.ping();return NextResponse.json({status:"ok",service:"ai-bid-mvp",database:"ok",queue:"ok",time:new Date().toISOString()})}catch(e){console.error(e);return NextResponse.json({status:"degraded",database:"error"},{status:503})}}